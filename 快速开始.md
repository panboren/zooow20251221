# 🚀 快速开始指南

## 5分钟上手

### 1. 创建第一个优化动画

```bash
# 生成动画模板
node scripts/AnimationTemplateGenerator.js MyFirstAnimation enhancedBaseEffect
```

### 2. 使用ParticleFactory创建粒子

```javascript
import { ParticleFactory } from '~/utils/ParticleFactory.js'

// 创建5000个螺旋粒子
const particles = ParticleFactory.create({
  count: 5000,
  shape: 'spiral',
  colorMode: 'gradient',
  color1: 0x00ffff,
  color2: 0xff00ff
})

scene.add(particles)
```

### 3. 使用性能监控面板

```javascript
import { createPerformancePanel } from '~/components/PerformancePanel.js'

// 创建监控面板
const panel = createPerformancePanel({
  position: 'top-right',
  showFPS: true,
  showMemory: true
})

// 更新性能数据
panel.update({
  fps: 58,
  memory: 180,
  particles: 5000,
  drawCalls: 12
})
```

### 4. 运行测试

```bash
# 运行所有测试
node scripts/AnimationTestSuite.js

# 运行性能基准测试
node scripts/PerformanceBenchmark.js

# 运行回归测试
node scripts/RegressionTest.js
```

## 常用命令

```bash
# 创建新动画
node scripts/AnimationTemplateGenerator.js <Name> <Type>

# 分析代码
node utils/quick-analyze.js

# 批量重构
node scripts/BatchRefactor.js

# 部署
node scripts/Deploy.js
```

## 文件位置

- 核心工具: `utils/`
- 脚本工具: `scripts/`
- 类型定义: `types/`
- 文档: `docs/`
- 动画基类: `pages/home/components/animation/animations/base/`

## 更多信息

- 完整文档: `完整优化总结.md`
- 工具指南: `docs/TOOL_GUIDE.md`
- 示例代码: `docs/EXAMPLES.md`
