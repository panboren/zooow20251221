# 项目架构说明

## 📁 目录结构优化

```
enterprise-product-website/
├── assets/                      # 静态资源
│   ├── images/                 # 图片资源
│   └── styles/                 # 样式文件 (重命名为 styles，语义更清晰)
│       ├── variables.scss      # 样式变量
│       └── main.scss          # 主样式文件
│
├── components/                 # Vue 组件
│   ├── layout/                # 布局组件
│   │   ├── TheHeader.vue     # 网站头部
│   │   └── TheFooter.vue     # 网站底部
│   ├── common/               # 通用组件
│   │   ├── BackToTop.vue    # 返回顶部
│   │   └── PageHeader.vue   # 页面头部
│   └── business/             # 业务组件
│       ├── ProductCard.vue  # 产品卡片
│       └── TestimonialCard.vue # 评价卡片
│
├── composables/              # 组合式函数 (逻辑复用)
│   ├── index.ts             # 统一导出
│   ├── useSeo.ts           # SEO 管理
│   ├── useProducts.ts      # 产品数据
│   ├── useArticles.ts      # 文章数据
│   └── useContact.ts       # 联系表单
│
├── layouts/                 # 布局模板
│   ├── default.vue         # 默认布局
│   └── home.vue            # 首页布局
│
├── pages/                  # 页面路由
│   ├── index.vue          # 首页
│   ├── products/          # 产品页面
│   │   ├── index.vue     # 产品列表
│   │   └── [slug].vue    # 产品详情
│   ├── news/             # 新闻页面
│   ├── solutions.vue     # 解决方案
│   ├── about.vue         # 关于我们
│   └── contact.vue       # 联系我们
│
├── types/                 # TypeScript 类型定义
│   └── index.ts         # 所有类型定义
│
├── server/               # 服务端代码
│   └── api/             # API 路由
│
├── public/              # 公共静态文件
│   └── images/         # 公共图片
│
├── nuxt.config.ts      # Nuxt 配置文件
├── package.json        # 依赖配置
├── tsconfig.json       # TypeScript 配置
└── README.md          # 项目说明
```

## 🎯 架构设计原则

### 1. 分层架构
```
┌─────────────────────────────────────┐
│         Pages (页面层)               │  - 页面组件，负责页面布局和业务逻辑
├─────────────────────────────────────┤
│      Components (组件层)             │  - 可复用 UI 组件，负责展示
├─────────────────────────────────────┤
│    Composables (逻辑层)              │  - 业务逻辑复用，状态管理
├─────────────────────────────────────┤
│       Server (服务层)               │  - API 接口，数据处理
└─────────────────────────────────────┘
```

### 2. 关注点分离
- **Pages**: 只负责页面布局和组合
- **Components**: 只负责 UI 展示，通过 props 接收数据
- **Composables**: 封装业务逻辑，提供状态和方法
- **Types**: 集中管理类型定义

### 3. 代码组织规范
- 每个文件只做一件事
- 相关功能放在同一目录
- 组件按功能分类（layout/common/business）
- 使用 TypeScript 类型约束

## 🔧 配置文件说明

### nuxt.config.ts
```typescript
// 模块配置
modules: [
  '@pinia/nuxt',        // 状态管理
  '@nuxt/image',        // 图片优化
  '@vueuse/nuxt',       // 工具函数
  '@nuxtjs/color-mode'  // 主题切换
]

// 运行时配置
runtimeConfig: {
  private: { apiSecret: '' },    // 服务端私有配置
  public: { siteName: '' }       // 客户端公共配置
}

// 路由规则（性能优化）
routeRules: {
  '/': { isr: 60 },              // ISR 60秒
  '/products/**': { isr: 3600 }  // ISR 1小时
}
```

### tsconfig.json
```json
{
  "paths": {
    "@/*": ["./*"],                  // 路径别名
    "@components/*": ["./components/*"],
    "@composables/*": ["./composables/*"],
    "@assets/*": ["./assets/*"],
    "@types/*": ["./types/*"]
  }
}
```

## 📦 Composables 设计规范

### 命名约定
- 使用 `use` 前缀：`useSeo`, `useProducts`
- 每个文件只导出一个 composable

### 标准结构
```typescript
// composables/useExample.ts
export const useExample = () => {
  // 响应式状态
  const data = ref([])
  const loading = ref(false)
  const error = ref<string | null>(null)

  // 方法
  const fetchData = async () => {
    // 实现逻辑
  }

  // 只读导出
  return {
    data: readonly(data),
    loading: readonly(loading),
    error: readonly(error),
    fetchData
  }
}
```

### 统一导出
```typescript
// composables/index.ts
export { useSeo } from './useSeo'
export { useProducts } from './useProducts'
// ...
```

## 🎨 组件设计规范

### 组件分类
- **Layout Components**: 布局组件（Header、Footer）
- **Common Components**: 通用组件（Button、Card）
- **Business Components**: 业务组件（ProductCard、TestimonialCard）

### Props 定义
```typescript
interface Props {
  title: string
  description?: string  // 可选属性
}

const props = withDefaults(defineProps<Props>(), {
  description: '默认值'
})
```

### 样式作用域
```vue
<style scoped lang="scss">
// 只作用于当前组件
.component-name {
  // 样式
}
</style>
```

## 📝 类型定义规范

### 类型分类
```typescript
// ============================================
// 实体类型
// ============================================
export interface Product { ... }
export interface Article { ... }

// ============================================
// API 类型
// ============================================
export interface ApiResponse<T> { ... }
export interface PaginatedResponse<T> { ... }

// ============================================
// 表单类型
// ============================================
export interface ContactForm { ... }

// ============================================
// 通用类型
// ============================================
export type Nullable<T> = T | null
export type Optional<T> = T | undefined
```

## 🚀 性能优化策略

### 1. 代码分割
- 页面级自动代码分割
- 动态导入大型组件

### 2. ISR（增量静态再生）
```typescript
nitro: {
  routeRules: {
    '/': { isr: 60 },              // 首页缓存60秒
    '/products/**': { isr: 3600 } // 产品缓存1小时
  }
}
```

### 3. 图片优化
```vue
<NuxtImg
  src="/image.jpg"
  format="webp"
  loading="lazy"
  width="800"
  height="600"
/>
```

### 4. 组件懒加载
```vue
<LazyMyComponent v-if="show" />
```

## 🔐 环境配置

### 开发环境
```bash
npm run dev
```

### 生产构建
```bash
npm run build
```

### 静态生成
```bash
npm run generate
```

## 📚 扩展指南

### 添加新页面
1. 在 `pages/` 目录创建 `.vue` 文件
2. Nuxt 自动生成路由

### 添加新组件
1. 在 `components/` 相应目录创建 `.vue` 文件
2. 组件自动导入，无需手动引入

### 添加新 Composable
1. 在 `composables/` 创建 `useXXX.ts`
2. 在 `composables/index.ts` 导出
3. 在页面中直接使用

### 添加 API
1. 在 `server/api/` 创建路由文件
2. 使用 `$fetch` 调用

## 🎯 最佳实践

1. **类型安全**: 始终使用 TypeScript
2. **组件复用**: 提取公共逻辑到 Composables
3. **样式隔离**: 使用 scoped 样式
4. **性能优先**: 合理使用 ISR 和懒加载
5. **代码规范**: 遵循统一的命名和结构约定
6. **注释文档**: 关键逻辑添加注释

## 📖 参考资料

- [Nuxt 3 官方文档](https://nuxt.com)
- [Vue 3 官方文档](https://vuejs.org)
- [TypeScript 文档](https://www.typescriptlang.org)
- [VueUse 文档](https://vueuse.org)
